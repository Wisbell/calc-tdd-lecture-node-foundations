// calculator should take in object and then call the necessary functions


const { assert: { equal } } = require('chai')
const calc = require('../lib/calculator')

describe('calculator', () => {
  it('should run add function if operation is "+"', () => {
    equal()
  })

})
