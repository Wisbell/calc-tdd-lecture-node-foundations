// const { assert: { equal, isArray, isNumber, isString } } = require('chai')
// const cli = require('../lib/cli')

// describe('cli', () => {
//   it('args should be an array', () => {
//     isArray(cli.args, "should be an array")
//     // console.log(cli().args)
//   })

//   it('argv should have 3 arguments', () => {
//     equal(3, cli.args.length)
//   })

//   it('1st arg in argv should be a number', () => {
//     isNumber(cli.args[0], 'should be a number')
//   })

//   it('2nd arg in argv should be an opperand', () => {
//     isString(cli.args[1], 'should be an string')
//   })

//   it('3rd arg in argv should be a number', () => {
//     isNumber(cli.args[2], 'should be a number')
//   })
// })
