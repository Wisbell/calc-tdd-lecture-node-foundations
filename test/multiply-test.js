// const { assert: { isNumber, equal } } = require('chai')
// const multiply = require('../lib/multiply')

// describe('multiply', () => {
//   it('should return a number', () => {
//     isNumber(multiply())
//   })

//   it('should return a multiple of two numbers', () => {
//     equal(4, multiply(2, 2))
//   })
// })
